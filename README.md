# Movie Booking Application (Daivtech Assessment)

A React Native mobile application for booking movie tickets.

## Instructions to Run

1. Run `npm install` to install dependencies
2. Run `npm start` to start the server
3. Open in Expo Go / Emulator to view the app

## 📌 Notes

- No backend used (as per instructions)
- Focused on functionality and navigation
- UI kept simple
